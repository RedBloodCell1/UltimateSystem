--!strict
-- Defining Data Template
return {
	-- CURRENCY
	Dollars = 0,

	-- GAMEPLAY STATS (We save levels)
	Upgrade = {
		ClickSpeedLevel = 1,
		PassiveIncomeLevel = 0,
		ClickPowerLevel = 1,
	},

	-- METADATA (Good Practice)
	LoginCount = 0,
	LastLogin = 0,
}
